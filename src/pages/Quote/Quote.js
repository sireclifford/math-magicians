import React from 'react';
import './Quote.css';

const Quote = () => (
  <div className="quote">
    <p>
      Mathematics is not about numbers, equations, computations, or algorithms.
      It is about the way we think about numbers, equations, computations.
    </p>
  </div>
);

export default Quote;
